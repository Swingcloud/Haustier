<li> 
	Name: <%= pet.name %> 
	<ul>
		<li>Age: <%= pet.age %></li>
		<% if @customer %>
			<li><%= link_to 'adopt this pet!', adopt_customer_path(@customer, pet_id: pet.id) %></li>
		<% else %>
			<li><%= link_to 'adopt this pet!', adopt_customer_path(customer, pet_id: pet.id) %></li>
		<% end  %>
	</ul>
</li>